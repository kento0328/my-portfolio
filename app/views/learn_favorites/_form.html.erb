<%= form_with(model: learn_favorite) do |form| %>
  <% if learn_favorite.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(learn_favorite.errors.count, "error") %> prohibited this learn_favorite from being saved:</h2>

      <ul>
        <% learn_favorite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.label :learn_id, style: "display: block" %>
    <%= form.number_field :learn_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
